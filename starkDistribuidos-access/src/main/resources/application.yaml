spring:
  application:
    name: stark-access
  datasource:
    url: jdbc:h2:mem:accessdb
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8084

