spring:
  application:
    name: stark-notification
  datasource:
    url: jdbc:h2:file:./data/notificationdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: ''
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: mypillsfp@gmail.com
    password: kfqulbdlzxppemzy
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.starttls.required: true
eureka:
  client:
    service-url:
      defaultZone: http://admin:admin123@127.0.0.1:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
server:
  port: 8085

