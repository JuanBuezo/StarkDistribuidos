spring:
  application:
    name: stark-alert
  datasource:
    url: jdbc:h2:mem:alertdb
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8083

