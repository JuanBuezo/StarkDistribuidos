spring:
  application:
    name: stark-alert
  datasource:
    url: jdbc:h2:mem:alertdb
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: true
eureka:
  client:
    service-url:
    fetch-registry: true
server:
  port: 8083

